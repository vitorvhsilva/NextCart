spring:
  application:
    name: writer

sqs:
  cart-writer-command-queue:
    name: cart-writer-command-queue

aws:
  region: us-east-1
  sqs:
    endpoint: http://localhost:4566
  access-key: test
  secret-key: test

spring.cloud.aws:
  sqs:
    endpoint: http://localhost:4566
    region: us-east-1
